# AngularMatSelectAutocomplete

This is a demo of Angular Material's `mat-select` component with custom autocomplete (search) functionality.

## Development server

Run `ng serve` for a dev server. Navigate to `http://localhost:4200/`.
